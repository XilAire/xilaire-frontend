// app/support/layout.tsx
export const metadata = {
  title: 'Support | XilAire',
  description: 'Get help, track tickets, and connect with XilAire support.',
};

export default function SupportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
